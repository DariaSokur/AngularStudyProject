import { TransformContactOnbjectToStringPipe } from './transform-contact-object-to-string.pipe';

describe('TransformContactOnbjectToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformContactOnbjectToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
